export declare function createCmd(tables: string[], cmdObj: {
    all: boolean;
}): Promise<void>;
export declare function seedCmd(tables: string[], cmdObj: {
    all: boolean;
}): Promise<void>;
export declare function extCmd(extensions: string[], cmdObj: {
    all: boolean;
}): Promise<void>;
export declare function relCmd(tables: string[], cmdObj: {
    all: boolean;
}): Promise<void>;
//# sourceMappingURL=common.d.ts.map