{
  "name": "@ffwd/db",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "directories": {
    "dist": "dist",
    "test": "__tests__"
  },
  "scripts": {
    "test": "echo \"Error: run tests from root\" && exit 1",
    "build": "rm -rf dist && tsc"
  },
  "dependencies": {
    "@databases/pg": "^5.0.0",
    "@ffwd/util": "workspace:*",
    "pg": "^8.6.0"
  },
  "devDependencies": {
  }
}
